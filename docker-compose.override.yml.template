# docker-compose.override.yml 模板
# 复制此文件为 docker-compose.override.yml 并修改密码

version: '3.8'

services:
  mysql:
    environment:
      # 请修改为您的实际密码
      MYSQL_ROOT_PASSWORD: YOUR_MYSQL_ROOT_PASSWORD
      MYSQL_PASSWORD: YOUR_MYSQL_PASSWORD
      
  # 如果需要，可以添加其他服务的覆盖配置
  # Redis 配置示例（如果使用）
  # redis:
  #   ports:
  #     - "6379:6379"
